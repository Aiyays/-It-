本地人脸识别管理平台开发文档
创建一个服务器
	有接受数据的功能：有一个人脸识别SDK,，这里因为我们是调用百度的SDK，所以都放在服务器，然后分别接受
	客户端的传递过来的照片，当接收到客户端照片

	
	


一个客户端---->>>>一个Winform程序
第一个功能
	有三块屏幕-->>第一块：实时监测，显示与摄像头实时跟踪 
				  第二块：显示已授权用户并且每次往后,依次添加,当有相同的
				  第三块：显示未授权用户,依次添加,当有相同的
第二个功能
	麻将桌一样的签到-->>首先需要一个 listView,
	
	监控画面上实时显示授权对画面进行抓拍（需要一个人脸检测的SDK）摄像机捕捉到人脸时会自动触发的一个事件（连拍10张-->防止一张取样不准确）然后对比,当10张中有一张成功,就算通过验证
	以后删除9张照片并记录通过验证的人的 
	
	
	PATH C:\Emgu\emgucv-windesktop 3.3.0.2824\bin;%PATH%
	
	C:\Emgu\emgucv-windesktop 3.3.0.2824\bin
	
数据库管理

学生信息表--StudentInformation
	学号	     StudentId     int(20)   not null  key 
	姓名	     StudentName   char(20)  not null 
	所属公寓名称 ApartmentName char(20)  not null 
	学院	     SchoolName    char(20)  not null
	辅导员工号	 InstructorNub char(20)  not null
	寝室号	     DormNumber    int(6)    not null
	自己的照片   StudentPhoto  char()    not null 
	
教职工信息表--TeachingStaff
	姓名     name      char(20)      not null 
	职工类型 StaffType char(10) not null 
	职工编号 StaffNub  int(20)  not null   key 
	职工照片 StaffPhoto char()  not null;
	
学生状态表--